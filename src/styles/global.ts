import { globalCss } from ".";

export const globaStyles = globalCss({
    '*' : {
        margin : 0,
        padding : 0,
    },

    body : {
        '-webkit-font-smoothing' : 'antialiased',
    }
})